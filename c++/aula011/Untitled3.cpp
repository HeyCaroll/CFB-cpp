#include <iostream>

using namespace std;
int main(){
	 int num;
	 num=90;
	 
	 if( (num >= 3 && num <=  8 ) || (num > 9 && num < 15) ){
	 	cout << "\nValor aceito\n";
	 }
	 else{
	 	cout<<"\nValor recusado\n";
	 }
	
	return 0;
}
